# Changelog

所有版本都有 GPG 簽章的 tag。每一版的完整說明在 `git show <tag>`。
Every release is a GPG-signed tag; `git show <tag>` has the full note.

格式：`版本 — 這一版修好的問題`。這個專案大部分的版本都是在修「畫面看起來
正常、功能卻壞掉」的那一類問題，所以標題寫的是症狀而不是功能名稱。


## v2.23.3 — 2026-08-02

**修正上一版對配對題的誤殺**

v2.23.2 加的「不支援 bodyHtml 的題型帶著它就報錯」下得太粗暴，
把配對題排流程圖／表格這種官方聽力常見版面也擋掉了。
配對題本來就支援 bodyHtml —— 每個空格會畫成下拉選單。
規則改成只擋真正會讓學生看不到題目的情況：
· 有題目既沒題幹、版面也沒有它的空格
· 空格指向不存在的題目（重新編號沒改版面）


## v2.23.2 — 2026-08-02

**聽力吞題目**

學生端無條件把 bodyHtml 當成題目版面，而驗證的空格檢查只對
gap_fill 系列做。於是一個 short_answer 或 mcq_single 題組只要帶著
一段殘留的 bodyHtml（換題型、匯入、AI 出題、題庫沿用都會發生），
整組題目就完全不會畫出來 —— 底部題號列照樣列出來。
實測 7 題的聽力卷被吞掉 5 題，沒有任何錯誤訊息。
四層修：不再吞題（已存進去的壞試卷也救得回來）、畫完之後對帳並


## v2.23.1 — 2026-08-02

**圖表爆版**

寫作 Task 1 的圖表完全沒有寬度限制（唯一那條 max-width 只管
.cbt-group 裡的圖，而 Task 1 的圖不在裡面）。1600×900 的長條圖
原尺寸畫出去，右邊超出容器 981px、下面超出視窗 276px，學生只
看得到左上角兩根長條。
四個出圖的地方統一走 figure()：縮到容器內、保持比例、不超過視窗
高度。另外加上點圖放大檢視（可縮放、可拖曳），寫作的分隔線也改成


## v2.23.0 — 2026-08-02

**班級隔離**

新增成員時可指定一位老師管哪些班，之後他只看得到那些班的學生。
不指定任何班級 = 全校範圍（科目負責人、教務、代課老師就這樣設；
升級後既有老師也不會突然被鎖在外面）。
四個限制點：看得到哪些學生、新增與編輯學生、指派考試、看與批改成績。
批次動作一律整批檢查，只要有一位越界就整批拒絕。
順帶把 /manage 的全站統計、維護紀錄、清理與整份備份收緊到只給


## v2.22.0 — 2026-08-02

**成員權限稽核**

角色本身的把關是乾淨的：垂直越權、水平越權、JWT 偽造全部擋得住。
破的是檔案這一塊，而且串起來可以直接拿到管理員權限：
1. 學生可上傳 .html 當作錄音，落地在同源、免驗證、Content-Type
   是 text/html 的位置 —— 老師點開就被偷走 token（全站無 CSP）
2. 任何人不用登入就能下載全校學生的口說錄音（檔名可預測）
3. 老師可刪掉任何試卷，連帶 cascade 掉所有學生的作答


## v2.21.3 — 2026-08-02

**全科目稽核**

六個會毀掉學生考試、但畫面上完全看不出來的問題：
1. 聽力畫一條螢光筆，音檔永久停止，那個 Part 的十題報銷
2. 存檔失敗時作答永久消失，題號列卻照樣顯示已作答
3. 交卷後成績永遠出不來（attempts 缺 updated_at，錯誤被吞掉）
4. 提示訊息蓋住底部的上下題與結束按鈕
5. 多選題答案寫成「A,D」時驗證全綠但全班 0 分


## v2.21.2 — 2026-08-02

**口說按鈕會自己跑掉**

學生回報「口說考試時所有按鈕點下去都沒有反應」。成因是按鈕
排在捲動區裡，逐字稿一長就整排往下跳（實測 60px）。按下去的
那一瞬間按鈕已經不在原位，mousedown 跟 mouseup 落在不同元素上，
瀏覽器不會發出 click —— 完全沒有錯誤，事後看畫面也一切正常。
按鈕改為釘在底部。順帶修好預設配色缺少 --c-danger（「結束測驗」
是整排裡最淡的一顆）、對話框畫不出來時的行內樣式保險，以及


## v2.21.1 — 2026-08-02

**對話框配色回歸修正**

上一版把對話框搬到 document.body 修好了「按鈕完全沒反應」，
卻讓它失去 .cbt 上的所有配色變數：沒底色、沒外框、沒陰影，
OK 也不像按鈕。這一版讓遮罩自己帶著 cbt 並複製主題屬性，
同時把顯示設定的套用範圍改成所有 .cbt，學生在對話框裡換配色
才會立刻看到效果。
新增 test/browser/dialogStyle.mjs（24 項 computed style 檢查），


## v2.21.0 — 2026-08-02

**按鈕完全沒反應、重整後對話全失**

對話框被 append 進會被重畫掉的容器，Promise 永遠不 resolve，
所有 await dlg(...) 的按鈕就此卡死。改掛 document.body。
重整後對話從 speaking_responses 重建（先前那份每三輪才寫一次，
講一兩句就重整的話是空的）。
新增真瀏覽器的口說模擬台，三種學生情境。
測試：單元 162 項、端對端 464 項。


## v2.20.2 — 2026-08-02

**選了選項之後整個網頁跳回最上面**

選項的 onclick 會把整科重畫，捲動位置歸零（實測從 1392px 掉到 0）。
改成就地更新選取狀態，移動 0px。
測試：單元 157 項、端對端 464 項。


## v2.20.1 — 2026-08-02

**最後一科結束就立刻收卷**

實測 2 分鐘的考試：頁面開著在第 122 秒顯示時間到、142 秒交卷；
分頁關掉則由伺服器在第 173 秒收掉。前者多等的 20 秒是在等掃描，
現在 module/finish 會直接收。
測試：單元 153 項、端對端 464 項。


## v2.20.0 — 2026-08-02

**考試時限完全靠學生的瀏覽器在執行**

倒數是拿伺服器的截止時間減學生電腦的時鐘 —— 電腦慢十分鐘就多考十分鐘。
而且時限沒有任何伺服器端的執行：分頁一關，那場考試永遠停在「考試中」。
現在前端會跟伺服器對時，伺服器每 30 秒自己掃描逾時並自動收卷批改。
測試：單元 152 項、端對端 464 項。


## v2.19.0 — 2026-08-02

**考官回一句之後就再也不出聲**

成因是 response.create 撞上端點自己發起的回應，而那則
already_active 錯誤在 v2.15.0 被當成協定雜訊濾掉了 ——
撞在階段轉換那一次，Part 1 的第一題就永遠問不出來。
改成排隊發話 + 會自救的看門狗，並明確禁止考官自己停下來。
測試：單元 145 項、端對端 454 項。


## v2.18.0 — 2026-08-02

**考官聽不到學生：找出成因，並讓這種失敗不再無聲**

音訊管線本身是好的（440Hz/24kHz/即時，已用已知訊號驗證）。成因是
Chrome 降噪把人聲壓掉 3/4，配上端點的語音偵測門檻，安靜的學生整段
都過不了。降噪預設關閉、門檻可調、並加上會講話的收音診斷。
另修：斷線期間的聲音不再直接丟掉、口說分數寫回成績單。
測試：單元 139 項、端對端 454 項。


## v2.17.0 — 2026-08-02

**AI 口說考官的訂製設定**

管理員設全站預設，老師指派時可針對班級覆寫。四組：人設與口音、
換手靈敏度、難度與追問、學生看得到什麼。
行為變更：即時分數預設改成不給學生看（真實考試不會邊考邊報分數）。
分數照常算、照常存，老師看得到；要恢復舊行為把那一項打開即可。
測試：單元 133 項、端對端 454 項。


## v2.16.0 — 2026-08-02

**口說即時對話：學生的聲音從來沒有送出去過**

音訊擷取的節點接到了一個沒有接到喇叭的 GainNode，Web Audio 因此完全不會拉
那條線 —— 學生的聲音從頭到尾沒有送出去，考官全程在自言自語。
「考官亂問」「轉錄稿沒有學生的聲音」「不流暢」全部由此而來。
另外修好：換手規則整場只設定一次（Part 2 長回答被考官插話）、
打斷考官的判斷放在前端（考官被自己的回音砍掉）、上游掉線學生看不到、
斷線重連把逐字稿蓋掉、學生斷線就等於這一科報銷、取樣率沒檢查、


## v2.15.1 — 2026-08-02

**反作弊的次數算法與上限機制**

切一次分頁被記成兩次、重新整理之後次數會跳上去、上限比設定的嚴格一級、
上限完全沒有強制力（只寫在前端）、「開始作答」被算成違規、
成績頁兩個數字互相矛盾 —— 六個問題全部修好並補上迴歸測試。
測試：單元 108 項、端對端 446 項。


## v2.15.0 — 2026-08-01

**口說控制鍵失效與六種「學生按不下去」的題目**

口說：進入下一部分／結束測驗按了沒反應、考官不接話時無聲卡住，都修好了，
並新增「叫考官接話」與 12 秒無回應提示。
題型：實測十一種題型從建卷走到檢討，修掉六種會讓學生完全不能作答的情況，
其中最嚴重的是「打開題目編輯器再存檔，單選題的選項就會整組消失」。
題目編輯器：補上閱讀原文、聽力音檔與逐字稿，以及寫作與口說的所有欄位
（這兩科原本在介面上完全改不了）。


## v2.14.1 — 2026-08-01

**通知不再無限累積**

送通知時就順手修剪：每人最近 200 則、已讀硬上限 180 天，
不依賴預設關閉的自動清理。另修「一個壞帳號害整批 200 人收不到」的 bug。


## v2.14.0 — 2026-08-01

**支援 OpenAI Realtime GA 協定**

拿掉 OpenAI-Beta 標頭、改用 GA 的 session.update 結構與事件名稱，
並保留 Beta 相容：預設自動偵測，先試 GA 被拒就退回 Beta。
另修連線失敗時錯誤訊息送不出去的問題。


## v2.13.0 — 2026-08-01

**全面稽核後的修正**

嚴重：POST /exam/start 沒有把關，學生送一個 testId 就能開任何試卷，
交卷後從 /results/:id 拿到整份標準答案與解析。已修並加上回歸測試。
另修：考試時限可被跳過、口說題卡提前外洩、自動組卷吃掉文章、
題號位移沒改 bodyHtml、寫作 Task 1 圖表沒顯示、口說評語漏畫、
exam_events 與 Docker 日誌無上限成長。


## v2.12.1 — 2026-08-01

**檢討時把選項清單也畫出來**

配合題、選字填空的選項是掛在題組上的，一組共用一份。
共用的畫在題組上方一次，單選題的畫在題目底下，
並標出「正解」與「你選的」；字母答案會補上對應的選項文字。


## v2.12.0 — 2026-08-01

**檢討看得到原文與圖片；診斷紀錄不再無限累積**

flattenQuestions 只留題幹，把文章、逐字稿、圖片全丟了，
成績單檢討、錯題複習、錯題重做三條路一起壞。新增 sectionMedia()
一節一份地帶回素材，同一篇文章只送一次。
考前診斷紀錄改成寫入時就修剪：每人 5 筆、每 IP 3 筆、90 天上限，
不依賴預設關閉的自動清理。


## v2.11.2 — 2026-08-01

**抓出被 Permissions-Policy 標頭擋掉的麥克風**

反向代理多加一份 microphone=() 會把系統送的允許蓋掉，
症狀跟「使用者自己封鎖」一模一樣，而且 Safari 完全正常。
現在會在要權限之前就判斷出來，並把實際收到的標頭列出來。


## v2.11.1 — 2026-08-01

**麥克風權限失敗時給得出可以照做的步驟**

先分辨是「網址不是 HTTPS」「瀏覽器記住封鎖」「只是把詢問視窗關掉」
還是「系統政策擋住」，再給對應的解法，並附上只重測麥克風的按鈕。


## v2.11.0 — 2026-08-01

**AI 出題可以先設難度**

把目標 Band 翻譯成具體的出題指令：文本長度與句構、難題型比例、
詞彙難度、聽力語速與口音。整份一個難度，可各科單獨覆寫，
四個面向也可個別微調，並即時用中文顯示這個設定會產出什麼。


## v2.10.0 — 2026-08-01

**考前環境檢查與紀律事件分級**

不用登入的 #/check 考前環境檢查：12 項檢查，麥克風連實際收音音量都量，
錯誤訊息講人話，附六碼診斷碼。開始考試前會強制跑一次，
麥克風權限因此在進全螢幕之前就拿到。
紀律事件分成 紀錄／需留意／可疑 三級，由伺服器判定。
修掉口說時離開全螢幕被記成違規的 bug（那一科本來就不要求全螢幕），
並讓處理裝置權限期間的離開不計入違規次數。


## v2.9.0 — 2026-08-01

**自動組卷與考試通知**

自動組卷：依目標題數／難度／題型從題庫抽題組卷，聽力 4 節、閱讀 3 篇，
題型自動分散、題號重新連續編號；湊不到目標時老實回報缺口，不硬塞也不重複用題組。
考試通知：頁首鈴鐺、指派與批改完成自動通知、老師手動發公告，已讀通知納入自動清理。
Email 通知（選用）：SMTP 設定頁與測試寄信，密碼只存伺服器並遮罩回傳。
同時修正手寫 SMTP 客戶端的三個協定錯誤（EHLO 從未送出、多行回應誤判、
TLS 升級前未拆監聽），並補上 Date 與 Message-ID 標頭。
